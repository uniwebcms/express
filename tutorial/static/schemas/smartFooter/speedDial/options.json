{
    "title": "options",
    "type": "object",
    "properties": {
        "maxCol": {
            "type": "number",
            "description": "Max number of column, each column display the related profiles in one page.",
            "default": "4"
        },
        "maxCount": {
            "type": "number",
            "description": "Max number of related profiles in each column.",
            "default": "3"
        },
        "bannerRound": {
            "type": "string",
            "enum": ["'none'", "'sm'", "'md'", "'lg'", "'xl'"],
            "description": "The round level of each profile's banner image.",
            "default": "none"
        },
        "bannerShape": {
            "type": "string",
            "enum": ["'rect'", "'sqar'"],
            "description": "The shape of each profile's banner image.",
            "default": "rect"
        }
    }
}
